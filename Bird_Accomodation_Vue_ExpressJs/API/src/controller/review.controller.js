const reviewModel = require('../models/review.model');

module.exports = {
    getAllReviews: async (req, res, next) => {
        try {
            // code here
        } catch (error) {
            next(error);
        }
    }
}